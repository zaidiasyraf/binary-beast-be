<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="009-create-customer-products-table" author="system">
        <createTable tableName="customer_products">
            <column name="customer_id" type="INTEGER">
                <constraints nullable="false"/>
            </column>
            <column name="product_id" type="INTEGER">
                <constraints nullable="false"/>
            </column>
            <column name="enrollment_date" type="DATE">
                <constraints nullable="true"/>
            </column>
            <column name="status" type="VARCHAR(50)">
                <constraints nullable="true"/>
            </column>
            <column name="usage_frequency" type="INTEGER">
                <constraints nullable="true"/>
            </column>
        </createTable>

        <addPrimaryKey tableName="customer_products" columnNames="customer_id, product_id"/>

        <addForeignKeyConstraint
                baseTableName="customer_products"
                baseColumnNames="customer_id"
                constraintName="fk_customer_products_customer"
                referencedTableName="customers"
                referencedColumnNames="customer_id"
                onDelete="CASCADE"/>

        <addForeignKeyConstraint
                baseTableName="customer_products"
                baseColumnNames="product_id"
                constraintName="fk_customer_products_product"
                referencedTableName="products"
                referencedColumnNames="product_id"
                onDelete="CASCADE"/>

        <sql>
            CREATE INDEX idx_customer_products_customer ON customer_products(customer_id);
            CREATE INDEX idx_customer_products_product ON customer_products(product_id);
        </sql>

        <rollback>
            <dropTable tableName="customer_products"/>
        </rollback>
    </changeSet>

</databaseChangeLog>

